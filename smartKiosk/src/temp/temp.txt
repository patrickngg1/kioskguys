/* Keeps Submit Request button visible at bottom inside modal */
.submit-request-sticky {
  position: sticky;
  bottom: 0;
  background: rgba(17, 24, 39, 0.85); /* matches modal tone */
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  padding: 0.5rem 1.25rem 1.25rem;
  z-index: 25;
  border-top: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 15px;
}